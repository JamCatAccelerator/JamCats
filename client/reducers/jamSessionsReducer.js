import * as types from '../constants/types';

const initialState = {
  newSong : '',
  userID: '',
  pastJamSessions: [],
  activeJamSession: [],
}

const jamSessionsReducer = (state = initialState, action) => {
  //add in different methods from action creators
  
  return state;
};

export default jamSessionsReducer;